export default function Games() {
    // this is all getting styled like it's part of the header - use more classes
    return (
        <main style={{ padding: "1rem 0" }}>
            <h2>Games</h2>
        </main>
    );
}